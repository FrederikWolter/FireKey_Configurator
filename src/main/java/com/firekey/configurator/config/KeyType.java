package com.firekey.configurator.config;

/**
 * Represents a type for a key. E.g. it is a navigation button, or it can handle a function call.
 */
public enum KeyType {
    NAV_UP,
    NAV_HOME,
    NAV_DOWN,
    ACTION
}
